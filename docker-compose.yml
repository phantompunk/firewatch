services:
  app:
    build: .
    environment:
      - DATABASE_URL=/data/firewatch.db?_pragma=journal_mode(WAL)&_pragma=busy_timeout(5000)&parseTime=true
    volumes:
      - firewatch_data:/data

volumes:
  firewatch_data:
